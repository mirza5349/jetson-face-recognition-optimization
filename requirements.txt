# FaceNet AGX Orin Benchmarking Suite Requirements
numpy>=1.22.0
opencv-python>=4.5.5.64
Pillow>=9.0.0
PyYAML>=6.0
pandas>=1.4.0
matplotlib>=3.5.0
pytest>=7.0.0
onnx>=1.12.0
# Note: On NVIDIA Jetson, PyTorch, ONNX Runtime (GPU), and TensorRT 
# should be installed using JetPack-compatible wheels or system packages.
# The requirements below represent the standard developer environment dependencies:
# torch
# torchvision
# onnxruntime-gpu
# pycuda (for TensorRT memory allocation)
